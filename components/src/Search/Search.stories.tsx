import React from "react";
import { Search } from "./Search";

export default {
  title: "ATOMS | Search / Search",
  component: Search,
};

export const SearchComponent = () => {
  return <Search />;
};
